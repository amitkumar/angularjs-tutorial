'use strict';

angular.module('angularjsTutorial')
.controller('AdminUsersCtrl', ['$scope', '$log', '$window',
  function ($scope, $log, $window) {
    $log.log('AdminUsersCtrl instantiated');
  }
]);
